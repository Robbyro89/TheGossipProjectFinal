<nav class="navbar navbar-expand-md navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= image_tag("LogoGossip.jpeg",alt: "Responsive image", alt: "Gossip", id: "logoG") %>
      <% if moussaillon_signed_in? %>
        <%= link_to 'Gossips On Rails', gossips_path, class: 'navbar-brand' %>
      <% else %>
        <%= link_to 'Gossips On Rails', root_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div id="navbar">
      <ul class="nav navbar-nav pull-right">
        <% if moussaillon_signed_in? %>
          <li class="dropdown">
            <a class="btn btn-outline dropdown-toggle" data-toggle="dropdown" href="#">
              Pandora Box
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to "Editer le Profil", edit_moussaillon_registration_path %>
              </li>
              <li>
                <%= link_to "Créer un Gossip", new_gossip_path %>
              </li>
              <li>
                <%= link_to "Déconnexion", destroy_moussaillon_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <div class="btn-group">
            <%= link_to "Se connecter", new_moussaillon_session_path, class:"btn btn-outline" %>
            <%= link_to "S'inscrire", new_moussaillon_registration_path, class:"btn btn-outline" %>
          <div>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
